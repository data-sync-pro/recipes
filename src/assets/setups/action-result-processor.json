{
  "title": "Action Result Processor",
  "blocks": [
    {
      "type": "p",
      "content": "An Apex extension point that runs after the Action in each transaction. Implement it to react to action outcomes — custom logging, follow-up processes, or branching logic based on per-record success or failure."
    },
    {
      "type": "p",
      "content": "The <code>process()</code> method receives a <code>List&lt;pushtopics.ActionResult&gt;</code>, one entry per record processed in that transaction, so you can iterate and act on each row's status, errors, and resulting IDs."
    },
    {
      "type": "h2",
      "content": "Interface Definition"
    },
    {
      "type": "code",
      "content": "global interface pushtopic.ActionResultProcessor {\n    void process(List<pushtopics.ActionResult> actionResults);\n}",
      "language": "apex"
    },
    {
      "type": "h2",
      "content": "How to Use"
    },
    {
      "type": "p",
      "content": "Implement <code>pushtopics.ActionResultProcessor</code>, then reference the class on the Executable:"
    },
    {
      "type": "image",
      "content": "image/apex/action-result-processor.png",
      "alt": "Reference the ActionResultProcessor class on the Executable"
    }
  ]
}
