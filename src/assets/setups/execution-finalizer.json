{
  "title": "Execution Finalizer",
  "blocks": [
    {
      "type": "p",
      "content": "An Apex extension point that runs custom logic immediately after an Execution completes — for both <strong>#Batch</strong> and <strong>#Data Loader</strong>. Implement it when you need to react to a completed run: send notifications, write audit or summary records, or trigger downstream processes."
    },
    {
      "type": "p",
      "content": "The <code>execute()</code> method receives the completed <code>Execution__c</code> record, so you can inspect its status, counts, and errors."
    },
    {
      "type": "h2",
      "content": "Interface Definition"
    },
    {
      "type": "code",
      "content": "global interface pushtopic.ExecutionFinalizer {\n    void execute(pushtopics__Execution__c execution);\n}",
      "language": "apex"
    },
    {
      "type": "h2",
      "content": "How to Use"
    },
    {
      "type": "p",
      "content": "Implement <code>pushtopics.ExecutionFinalizer</code>, then reference the class on the Executable:"
    },
    {
      "type": "image",
      "content": "image/apex/execution-finalizer.png",
      "alt": "Reference the ExecutionFinalizer class on the Executable"
    }
  ]
}
