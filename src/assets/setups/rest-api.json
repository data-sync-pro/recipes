{
  "title": "Overview",
  "blocks": [
    {
      "type": "p",
      "content": "Use the Data Sync Pro REST API to run Pipelines and Executables outside the DSP user interface. Any tool or system that can send an HTTP request can call these endpoints."
    },
    {
      "type": "table",
      "columns": [
        { "key": "operation", "header": "Operation", "width": "36%" },
        { "key": "mode", "header": "Mode", "width": "16%" },
        { "key": "response", "header": "Response", "width": "48%" }
      ],
      "rows": [
        { "operation": "Run a Pipeline", "mode": "Batch", "response": "Pipeline Execution record ID" },
        { "operation": "Run an Executable", "mode": "Batch", "response": "Execution record ID" },
        { "operation": "Run an Executable", "mode": "Sync", "response": "Per-record processing results" }
      ]
    },
    {
      "type": "p",
      "content": "**Batch** requests submit the execution for asynchronous processing. **Sync** requests process the records immediately and return the results in the response."
    }
  ]
}
