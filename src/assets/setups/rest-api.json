{
  "title": "Execute Pipeline and Executable via REST API",
  "blocks": [
    {
      "type": "p",
      "content": "Data Sync Pro exposes REST API endpoints that allow you to trigger pipeline and executable runs outside of the DSP UI. Any tool or system that can send an HTTP request can call them."
    },
    {
      "type": "h2",
      "content": "Calling via Postman",
      "children": [
        {
          "type": "callout",
          "variant": "info",
          "content": "**Prerequisites —** An External Client App in the target org with OAuth enabled and Postman's callback URL allowed."
        },
        {
          "type": "steps",
          "stepItems": [
            { "content": "Open Postman and set the request method to **POST**." },
            {
              "content": "Enter one endpoint URL from below:",
              "children": [
                {
                  "type": "keyvalue",
                  "keyStyle": "plain",
                  "pairs": [
                    { "key": "Pipeline (Batch)", "value": "{org_base_url}/services/apexrest/pushtopics/execution/batch/pipeline/{pipelineApiName}" },
                    { "key": "Executable (Batch)", "value": "{org_base_url}/services/apexrest/pushtopics/execution/batch/executable/{executableApiName}" },
                    { "key": "Executable (Sync)", "value": "{org_base_url}/services/apexrest/pushtopics/execution/sync/executable/{executableApiName}" }
                  ]
                }
              ]
            },
            {
              "content": "Under the **Authorization** tab, select **OAuth 2.0** and configure:",
              "children": [
                {
                  "type": "keyvalue",
                  "keyStyle": "plain",
                  "pairs": [
                    { "key": "Grant Type", "value": "Authorization Code (With PKCE)" },
                    { "key": "Auth URL", "value": "{org_base_url}/services/oauth2/authorize" },
                    { "key": "Access Token URL", "value": "{org_base_url}/services/oauth2/token" },
                    { "key": "Client ID", "value": "Your External Client App's Consumer Key" },
                    { "key": "Client Secret", "value": "Your External Client App's Consumer Secret" },
                    { "key": "Code Challenge Method", "value": "SHA256" }
                  ]
                }
              ]
            },
            { "content": "Click **Get New Access Token** and log in to Salesforce." },
            { "content": "Once authentication is complete, click **Proceed** to save the token." },
            { "content": "In **Manage Access Tokens**, click **Use Token**." },
            {
              "content": "Under the **Body** tab, select **raw › JSON**, and enter the request body:",
              "children": [
                {
                  "type": "card",
                  "title": "Pipeline",
                  "children": [
                    {
                      "type": "p",
                      "content": "`additionalRetrieveParameters` is optional. Use `null` for no additional filtering, or a condition string (e.g. `\"CreatedDate = TODAY\"`) to filter retrieved records."
                    },
                    {
                      "type": "code",
                      "language": "json",
                      "content": "{\"additionalRetrieveParameters\": null}"
                    }
                  ]
                },
                {
                  "type": "card",
                  "title": "Executable",
                  "children": [
                    {
                      "type": "p",
                      "content": "`sourceRecordIds` — an array of record IDs from the executable's source object."
                    },
                    {
                      "type": "code",
                      "language": "json",
                      "content": "{\"sourceRecordIds\": [\"001XXXXXXXXXXXX\"]}"
                    }
                  ]
                }
              ]
            },
            { "content": "Click **Send**." }
          ]
        },
        {
          "type": "h4",
          "content": "Response",
          "children": [
            {
              "type": "p",
              "content": "**Batch (Pipeline and Executable)** returns an execution record ID (e.g. `\"a0CDC000003DPrY2AW\"`).This means the batch job has been submitted, not that the run has finished. To check the run status, go to the corresponding **Pipeline Execution** or **Execution** record."
            },
            {
              "type": "p",
              "content": "**Sync (Executable)** returns the sync result for each record directly in the response body. No execution records are generated. Example response:"
            },
            {
              "type": "code",
              "language": "json",
              "content": "[\n    [\n        {\n            \"targetId\": \"006XXXXXXXXXXXX\",\n            \"success\": true,\n            \"sourceId\": \"006XXXXXXXXXXXX\",\n            \"skipped\": false,\n            \"action\": \"insert\"\n        }\n    ]\n]"
            }
          ]
        }
      ]
    },
    {
      "type": "h2",
      "content": "Calling via Apex",
      "children": [
        {
          "type": "callout",
          "variant": "info",
          "content": "**Prerequisite —** A **Named Credential** pointing to the DSP org must be configured in the calling org."
        },
        { "type": "h4", "content": "Pipeline Example" },
        {
          "type": "code",
          "language": "apex",
          "content": "HttpRequest request = new HttpRequest();\nrequest.setEndpoint('callout:Your_Named_Credential/services/apexrest/pushtopics/execution/batch/pipeline/YOUR_PIPELINE_API_NAME');\nrequest.setMethod('POST');\nrequest.setHeader('Content-Type', 'application/json');\nrequest.setTimeout(60000);\nrequest.setBody('{\"additionalRetrieveParameters\": null}');\nHttp http = new Http();\nHttpResponse response = http.send(request);\nSystem.debug('status: ' + response.getStatusCode());\nSystem.debug('response: ' + response.getBody());"
        },
        { "type": "h4", "content": "Executable Example" },
        {
          "type": "code",
          "language": "apex",
          "content": "HttpRequest request = new HttpRequest();\nrequest.setEndpoint('callout:Your_Named_Credential/services/apexrest/pushtopics/execution/sync/executable/YOUR_EXECUTABLE_API_NAME');\nrequest.setMethod('POST');\nrequest.setHeader('Content-Type', 'application/json');\nrequest.setTimeout(60000);\nrequest.setBody('{\"sourceRecordIds\": [\"001XXXXXXXXXXXX\"]}');\nHttp http = new Http();\nHttpResponse response = http.send(request);\nSystem.debug('status: ' + response.getStatusCode());\nSystem.debug('response: ' + response.getBody());"
        },
        {
              "type": "p",
              "content": "To run an executable asynchronously with execution logs, use the /batch/executable/ path with the same request body."
        },
        { "type": "label", "content": "Replace" },
        {
          "type": "keyvalue",
          "keyStyle": "code",
          "pairs": [
            { "key": "Your_Named_Credential", "value": "Named Credential pointing to the DSP org" },
            { "key": "YOUR_PIPELINE_API_NAME", "value": "The Pipeline's API name" },
            { "key": "YOUR_EXECUTABLE_API_NAME", "value": "The Executable's API name" },
            { "key": "001XXXXXXXXXXXX", "value": "Actual source record IDs in the DSP org" }
          ]
        }
      ]
    }
  ]
}
