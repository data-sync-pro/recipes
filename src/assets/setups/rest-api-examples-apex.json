{
  "title": "Apex",
  "blocks": [
    {
      "type": "callout",
      "variant": "info",
      "content": "**Prerequisite —** Configure a **Named Credential** in the calling org that points to the org where DSP is installed."
    },
    { "type": "h4", "content": "Run a Pipeline — Batch" },
    {
      "type": "code",
      "language": "apex",
      "content": "String namedCredentialApiName = 'namedCredentialApiName';\nString pipelineApiName = 'pipelineApiName';\n\nHttpRequest request = new HttpRequest();\nrequest.setEndpoint(\n    'callout:' + namedCredentialApiName +\n    '/services/apexrest/pushtopics/execution/batch/pipeline/' +\n    EncodingUtil.urlEncode(pipelineApiName, 'UTF-8')\n);\nrequest.setMethod('POST');\nrequest.setHeader('Content-Type', 'application/json');\nrequest.setTimeout(60000);\nrequest.setBody(\n    JSON.serializePretty(\n        new Map<String, Object>{\n            'additionalRetrieveParameters' => 'CreatedDate = TODAY',\n            'chainNextPipeline' => true\n        }\n    )\n);\n\nHttpResponse response = new Http().send(request);\nSystem.debug('Status: ' + response.getStatusCode());\nSystem.debug('Response: ' + response.getBody());"
    },
    { "type": "h4", "content": "Run an Executable — Batch" },
    {
      "type": "code",
      "language": "apex",
      "content": "String namedCredentialApiName = 'namedCredentialApiName';\nString executableApiName = 'executableApiName';\n\nHttpRequest request = new HttpRequest();\nrequest.setEndpoint(\n    'callout:' + namedCredentialApiName +\n    '/services/apexrest/pushtopics/execution/batch/executable/' +\n    EncodingUtil.urlEncode(executableApiName, 'UTF-8')\n);\nrequest.setMethod('POST');\nrequest.setHeader('Content-Type', 'application/json');\nrequest.setTimeout(60000);\nrequest.setBody(\n    JSON.serialize(\n        new Map<String, Object>{\n            'sourceRecordIds' => new List<String>{\n                '001XXXXXXXXXXXX'\n            }\n        }\n    )\n);\n\nHttpResponse response = new Http().send(request);\n\nSystem.debug('Status: ' + response.getStatusCode());\nSystem.debug('Response: ' + response.getBody());"
    }
  ]
}
